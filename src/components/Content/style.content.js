import { makeStyles } from "@material-ui/core";

export default makeStyles({
  listWrapper: {
    display: "flex",
    alignItems: "flex-start",
    flexWrap: "no-wrap",
    height: "90vh",
    maxWidth: "100vw",
  },
});
